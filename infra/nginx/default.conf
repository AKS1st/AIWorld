server {
    listen 80;
    server_name _;

    location /health {
        add_header Content-Type text/plain;
        return 200 'ok';
    }

    location / {
        add_header Content-Type application/json;
        return 200 '{"service":"AIWorld Gateway","status":"running"}';
    }
}
